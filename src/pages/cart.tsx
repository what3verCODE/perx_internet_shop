import {MainLayout} from "../layouts";
import {CartGrid} from "../components/cart/CartGrid";

export const CartPage = () => {
    return (
        <MainLayout>
            <CartGrid />
        </MainLayout>
    )
}